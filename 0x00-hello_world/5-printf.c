#include <stdio.h>
/**
 * main - prints the phrase with propoer grammerm
 * but the outcome is a piece of art
 * Return: 0 if exited proportly, non zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
